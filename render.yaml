# Render Blueprint — Cyclus Privacy Policy (static site)
services:
  - type: web
    name: cyclus-privacy
    runtime: static
    plan: free
    branch: main
    buildCommand: "echo ready"
    staticPublishPath: ./privacy-site
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
